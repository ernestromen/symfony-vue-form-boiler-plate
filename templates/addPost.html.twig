

{% block body %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
{# 
{{ form_start(form) }}
    <div class="form-group W-50 m-auto pb-4">
        {{ form_label(form.post) }}
        {{ form_widget(form.post, {'attr': {'class': 'form-control'}}) }}
    </div>
{{ form_end(form) }} #}
    <h1 class="text-center mt-5">Posts</h1>
    {# <table class="m-auto">
    <thead>
        <tr>
            <th>ID</th>
            <th>Post</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        {% for post in posts %}
            <tr class="border border-dark">
                <td class="border-right border-dark">{{ post.id }}</td>
                <td>{{ post.post }}</td>
                <td>
                <a class="btn btn-success " href="{{ path('edit_post', {'id': post.id}) }}" formmethod="delete">Edit post</a>
                </td>
                <td>
                <a class="btn btn-danger " href="{{ path('delete_post', {'id': post.id}) }}" formmethod="delete">Delete post</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table> #}
  <div id="app">
    <App />
</div>
{{ encore_entry_script_tags('app') }}
{% endblock %}
  
